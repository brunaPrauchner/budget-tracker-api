version: "3.9"
services:
  db:
    image: postgres:16
    container_name: budget-tracker-db
    environment:
      POSTGRES_DB: budgettracker
      POSTGRES_USER: budget_user
      POSTGRES_PASSWORD: budget_pass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U budget_user -d budgettracker"]
      interval: 5s
      timeout: 3s
      retries: 5
volumes:
  pgdata:
